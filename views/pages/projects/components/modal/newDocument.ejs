<div class="modal fade" id="addDocument" tabindex="-1" role="dialog" aria-labelledby="DocumentLabel"
    aria-hidden="true">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="DocumentLabel">Adicionar documento ao projeto</h5>
                <button class="close" type="button" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">×</span>
                </button>
            </div>
            <div class="modal-body" style="padding: 40px;">
                <form method="post">
                    <div class="mb-3">
                        <label class="small mb-1" for="documentName">Título do documento</label>
                        <input type="text" name="type" value="CREATE_DOCUMENT" style="display: none;">
                        <input name="documentName" class="form-control" id="documentName" type="text" placeholder="A.R.T"
                            required>
                            <input id="inputFile" accept="application/pdf" type="file" onchange="convertToBase64();" style="margin-top: 20px"/>
                            <input type="text" name="documentBase64" id="documentBase64" style="display: none;">
                    </div>
                    <small>Seja breve com o título, utilize se possível apenas uma palavra. Exemplo: ART, SOLICITAÇÃO, PROJETO, CONTRATO, etc...</small>
                    <button class="btn btn-primary" style="width: 100%;" type="submit">Adicionar Documento</button>
                </form>
            </div>
            <div class="modal-footer">
                <button class="btn btn-secondary" type="button" data-dismiss="modal">Cancelar</button>
            </div>
        </div>
    </div>
</div>


<script type="text/javascript">
    function convertToBase64() {
        var selectedFile = document.getElementById("inputFile").files;
        if (selectedFile.length > 0) {
            var fileToLoad = selectedFile[0];
            var fileReader = new FileReader();
            var base64;
            fileReader.onload = function(fileLoadedEvent) {
                base64 = fileLoadedEvent.target.result;
                console.log(base64);
                document.getElementById('documentBase64').value = base64;
            };
            fileReader.readAsDataURL(fileToLoad);
        }
    }
</script>